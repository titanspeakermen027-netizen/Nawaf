# Nawaf — Requested Features

## Prefix economy
- `C` — عرض رصيدك بصيغة مختصرة.
- `C @user` أو `C id` — عرض رصيد عضو.
- `C @user المبلغ` أو `C id المبلغ` — تحويل العملات.
- التحويل يحتاج تحققاً بصرياً برقم عشوائي، والمهلة 60 ثانية.
- لا يتم خصم العملة إلا بعد نجاح التحقق.
- عند نجاح التحويل تُحذف صورة التحقق ورسالة الرقم ثم تُرسل نتيجة التحويل كـReply بدون منشن للمرسل، وبعدها تُحذف رسالة التحويل الأصلية.

## Tickets
- Panel قابل للتخصيص، مع تحديد Category وLog Room.
- عند فتح التذكرة تظهر رسالة ترحيب داخل التذكرة.
- إغلاق التذكرة من طرف الإدارة فقط لصاحبها، وصاحب التذكرة لا يغلقها بنفسه.
- التقييم من 1 إلى 10 مع ملاحظة.
- `-تقييم` لفتح واجهة التقييم من داخل التذكرة المغلقة.
- حذف التكت للإدارة فقط.
- أزرار التكت والتقييم تبقى مسجلة بعد إعادة تشغيل البوت.

## Applications
- أنواع تقديم متعددة.
- أسئلة قابلة للتعديل حتى 10 أسئلة لكل نوع.
- صورة التقديم اختيارية.
- تحديد روم مراجعة وروم نتائج ورتبة عند القبول.
- قبول أو رفض مع سبب وإشعار للمتقدم.

## Levels
- XP وLevels للأعضاء.
- ربط Level محدد برتبة تلقائية.
- إمكانية إنشاء رتبة خاصة للمستوى الذي يسمح بإرسال الصور.

## Currency / Shop
- عملة خاصة بكل سيرفر مع اسم ورمز قابلين للتغيير.
- Balance، Transfer، Top، إضافة، حذف، تحديد وتصفير الرصيد.
- متجر لمنتجات عامة، رتب، وإعلانات.
- شراء الإعلان يطلب التفاصيل والصورة ويرسل الطلب إلى روم الطلبات.

## Moderation / Jail
- تحديد رتبة السجن وروم السجن من `/jail-settings`.
- عند السجن تزال الرتب القابلة لإدارة البوت ويضاف Jail Role.
- Jail Role يمنع رؤية باقي الرومات ويُسمح له فقط في روم السجن.
- `/jail` و`/unjail` مع دعم مدة السجن والفك التلقائي.
- `سجن @user` أو `سجن id` للاختصار.
- `عفو @user` أو `عفو id` لفك السجن واسترجاع الرتب.

## Messaging
- إرسال رسالة خاصة لعضو.
- إرسال رسالة إلى روم محدد.
- ردود prefix تكون Reply بدون Mention للعضو صاحب الأمر.

## Dhikr / Announcements
- أذكار تلقائية مع روم وفاصل زمني قابلين للتعديل.
- إعلانات موجهة إلى روم محدد.

## Stack
Python / discord.py 2.x / SQLite / Python 3.13 compatible dependencies.
