## Start

Create `.env` from `config.example.env`, put your Discord bot token in `DISCORD_TOKEN`, install `requirements.txt`, then run `python main.py`.

The database file `nawaf.sqlite3` is created automatically and is ignored by git.
